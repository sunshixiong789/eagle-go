version: v2

# 各服务内部配置 proto 的生成（app/*/internal/conf/conf.proto）。
# 与对外契约分开生成：配置结构不属于 API 契约，不需要 http/errors/openapi 插件。
inputs:
  - directory: app

plugins:
  - local: ["go", "tool", "protoc-gen-go"]
    out: app
    opt: paths=source_relative
